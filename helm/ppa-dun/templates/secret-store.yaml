apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: gcp-secret-store
spec:
  provider:
    gcpsm:
      projectID: v1-mvp
      auth:
        workloadIdentity:
          clusterLocation: us-east1
          clusterName: ppa-dun-cluster
          serviceAccountRef:
            name: app-secrets-ksa
